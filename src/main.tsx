import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import { SplineSceneBasic } from '../components/ui/code.demo.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SplineSceneBasic />
  </React.StrictMode>,
)
